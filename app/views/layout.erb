<!DOCTYPE html>
<html>
  <head>
    <title>Sinatra Pilot Logbook</title>
  </head>
  <body>
    <center>
      <div align="right">
        <a href="/"><input type="submit" value="Home"></a>
      </div>
      
      <div align="right">
        <a href="/logout"><input type="submit" value="Logout"></a>
      </div>
      <%= yield %>
      <h5><em><%= current_user ? "You're currently logged in as: #{current_user.username}" : nil %></em></h5>
    </center>
  </body>
</html>